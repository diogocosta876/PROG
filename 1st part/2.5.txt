#include iostream
#include iomanip
#include cmath
using namespace std;

int main()
{
    double a, b, c, r, i;
    double factor, x1, x2;
    cout  Solution of ax^2 + bx + c = 0  endl;
    cout  coeficient a ; cin  a;
    cout  coeficient b ; cin  b;
    cout  coeficient c ; cin  c;

    factor = b  b - 4  a  c;
    if (factor  0) {
        r = -b  (2  a);
        i = sqrt(-factor)  (2  a);
        cout  fixed  setprecision(3)  2 complex roots   r   +   i  i   and   r   -   i  i  endl;
    }
    else {
        x1 = (-b + sqrt(factor))  (2  a);
        x2 = (-b - sqrt(factor))  (2  a);
        if (x1 == x2) {
            cout  fixed  setprecision(3)  2 equal real roots      x1  endl;
        }
        else {
            cout  fixed  setprecision(3)  2 real roots    x1   and   x2  endl;
        }
    }
    return 0;
}