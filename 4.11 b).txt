#include <iostream>
#include <iomanip>
#include <string>
#include <algorithm>
#include <vector>
#include <climits>
#include <cmath>
using namespace std;

int compare(const void* a, const void* b) {
	return (*(int*)b - *(int*)a);
}

int main() {
	int a[5]{ 2,5,7,2,1 };
	qsort(a, 5, sizeof(int), compare );
	for (int i = 0; i < 5; i++) {
		cout << a[i] << " ";
	}
	cout << "\n";
	return 0;
}